@model RiftData.Presentation.ViewModels.Admin.GenusTypeCreatePageViewModel
    @using Telerik.Web.Mvc.UI
@{
    ViewBag.Title = "Create";
    Layout = "../Shared/_Layout.cshtml";
}

@section Navigation
{
    @Html.Partial("NavigationPartial", Model.SelectedView)
}

@section Scripts
{
    <script src="../../Scripts/GenusTypeUpdate.js" type="text/javascript"></script>
}
<div id="body">
    <div class="portlet">
        <div class="portlet-header">
            <ul id ="PageHeaderList">
                <li class="left">
                    <h4>Create Genus Type</h4>
                </li>
                <li class="right headerButton">
                    <a href="@Url.Action("Index")">
                        <button class="btn-sec">
                            <span>Back to list</span>
                        </button>
                    </a>
                </li>
            </ul>
        </div>
        <div class="portlet-content">
            @using (Html.BeginForm("Create", "GenusTypes", FormMethod.Post, new { @class = "form label-inline" }))
            {
                <div class="field">
                    <label>Name</label>
                    @Html.TextBox("Name", null, new { @class = "validate[required]" })
                </div>
                <div class="field">
                    <label>Lake</label>
                    @Html.Telerik().DropDownList().BindTo(Model.Lakes).Name("Lake")
                </div>
                <div class="buttonrow">
                    <button type="submit">
                        <span>Save</span>
                    </button>
                </div>
            }
        </div>
    </div>
</div>
<input type="hidden" value="@Url.Action("UpdateSuccess", "AjaxMessaging", new { subject = "Genus Type", okAction = "Index", okController = "GenusTypes" })" id="SuccessUrl" />
<input type="hidden" value="@Url.Action("UpdateFailure", "AjaxMessaging", new { subject = "Genus Type", okAction = "Index", okController = "GenusTypes" })" id="FailureUrl" />
@Html.Partial("MessageBox")

